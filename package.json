{
  "name": "mern-auth",
  "version": "1.0.0",
  "description": "MERN Authentication App",
  "private": true,
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "build": "npm install --prefix client && cd client && npx --yes vite build",
    "vercel-build": "npm install --prefix client && cd client && npx --yes vite build",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "engines": {
    "node": "18.x"
  }
}
